---
phase: quick-260608-f5y
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/ailang-core/src/lib.rs
  - crates/ailang-core/Cargo.toml
autonomous: true
requirements: [QUICK-F5Y]

must_haves:
  truths:
    - "cargo test --workspace passes with ≥22 tests (6 graph + 16 from tasks 0001-0003)"
    - "lib.rs declares all six modules: node_id, node_id_tests, ty, ty_tests, unify, unify_tests, graph, graph_tests"
    - "Cargo.toml for ailang-core retains [package] section with correct name/version/edition"
    - "Clean commit pushed to GitHub"
  artifacts:
    - path: "crates/ailang-core/src/lib.rs"
      provides: "Module registry for ailang-core"
      contains: "pub mod node_id"
    - path: "crates/ailang-core/Cargo.toml"
      provides: "Crate manifest"
      contains: "[package]"
  key_links:
    - from: "crates/ailang-core/src/lib.rs"
      to: "crates/ailang-core/src/node_id_tests.rs"
      via: "#[cfg(test)] mod node_id_tests"
      pattern: "mod node_id_tests"
    - from: "crates/ailang-core/src/lib.rs"
      to: "crates/ailang-core/src/ty_tests.rs"
      via: "#[cfg(test)] mod ty_tests"
      pattern: "mod ty_tests"
    - from: "crates/ailang-core/src/lib.rs"
      to: "crates/ailang-core/src/unify_tests.rs"
      via: "#[cfg(test)] mod unify_tests"
      pattern: "mod unify_tests"
---

<objective>
Repair the ailang-core crate after the orchestrator's task 0004 executor overwrote lib.rs,
dropping the test module declarations for tasks 0001-0003. The workspace builds and graph
tests pass (6), but 16 tests from node_id, ty, and unify are invisible because their
`mod` declarations were stripped from lib.rs.

Purpose: Restore the full 22-test baseline before any further orchestration work.
Output: Corrected lib.rs, verified ≥22 passing tests, clean git commit pushed to GitHub.
</objective>

<execution_context>
@/Users/cisco/.claude/get-shit-done/workflows/execute-plan.md
@/Users/cisco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/cisco/Documents/1_Progetti/ailang/.planning/PROJECT.md
@/Users/cisco/Documents/1_Progetti/ailang/.planning/ROADMAP.md
@/Users/cisco/Documents/1_Progetti/ailang/.planning/STATE.md

<interfaces>
<!-- Current (broken) lib.rs — missing test module declarations -->
pub mod node_id;
pub mod ty;
pub mod unify;
pub mod graph;
#[cfg(test)]
mod graph_tests;

<!-- Source files that exist on disk but are NOT wired into lib.rs -->
crates/ailang-core/src/node_id_tests.rs   ← exists, not declared
crates/ailang-core/src/ty_tests.rs        ← exists, not declared
crates/ailang-core/src/unify_tests.rs     ← exists, not declared

<!-- Correct Cargo.toml (already intact — verify not corrupted) -->
[package]
name = "ailang-core"
version = "0.1.0"
edition = "2021"

[dependencies]
blake3.workspace = true
thiserror.workspace = true

<!-- Cargo binary path (cargo not on $PATH in this environment) -->
RUSTUP_HOME=/Users/cisco/.rustup
CARGO_HOME=/Users/cisco/.cargo
PATH="/Users/cisco/.rustup/toolchains/stable-aarch64-apple-darwin/bin:$PATH"
cargo binary: /Users/cisco/.rustup/toolchains/stable-aarch64-apple-darwin/bin/cargo
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore lib.rs with all module declarations</name>
  <files>crates/ailang-core/src/lib.rs</files>
  <action>
Overwrite crates/ailang-core/src/lib.rs with the correct content that declares all six
production modules and all three legacy test modules (kept as #[cfg(test)] mod to match
the file-per-module pattern established in tasks 0001-0003):

```rust
pub mod node_id;
pub mod ty;
pub mod unify;
pub mod graph;

#[cfg(test)]
mod node_id_tests;
#[cfg(test)]
mod ty_tests;
#[cfg(test)]
mod unify_tests;
#[cfg(test)]
mod graph_tests;
```

Also verify Cargo.toml still has [package] section. If the [package] header is missing,
restore the full content shown in the interfaces block above. Do NOT alter the [dependencies]
section or add anything not already present.
  </action>
  <verify>
    <automated>RUSTUP_HOME=/Users/cisco/.rustup CARGO_HOME=/Users/cisco/.cargo PATH="/Users/cisco/.rustup/toolchains/stable-aarch64-apple-darwin/bin:$PATH" cargo test --workspace --manifest-path /Users/cisco/Documents/1_Progetti/ailang/Cargo.toml 2>&1 | grep -E "^test result"</automated>
  </verify>
  <done>All test result lines show 0 failures. The ailang-core test result line shows ≥22 passed.</done>
</task>

<task type="auto">
  <name>Task 2: Commit and push clean state</name>
  <files></files>
  <action>
Stage and commit only the files that changed:
  git -C /Users/cisco/Documents/1_Progetti/ailang add crates/ailang-core/src/lib.rs crates/ailang-core/Cargo.toml

Commit message:
  fix: restore lib.rs module declarations after task-0004 executor collision

  Executor overwrote lib.rs dropping node_id_tests, ty_tests, unify_tests mods.
  22 tests restored (6 graph + 16 from tasks 0001-0003).

Then push:
  git -C /Users/cisco/Documents/1_Progetti/ailang push

If Cargo.toml was NOT changed (it was already correct), omit it from the git add.
  </action>
  <verify>
    <automated>git -C /Users/cisco/Documents/1_Progetti/ailang log --oneline -1 && git -C /Users/cisco/Documents/1_Progetti/ailang status</automated>
  </verify>
  <done>git log shows the fix commit as HEAD. git status shows clean working tree. Remote is up to date.</done>
</task>

</tasks>

<verification>
Run full workspace test suite and confirm count:

  RUSTUP_HOME=/Users/cisco/.rustup CARGO_HOME=/Users/cisco/.cargo \
  PATH="/Users/cisco/.rustup/toolchains/stable-aarch64-apple-darwin/bin:$PATH" \
  cargo test --workspace --manifest-path /Users/cisco/Documents/1_Progetti/ailang/Cargo.toml \
  2>&1 | grep -E "^test result|FAILED"

Expected: one test result line for ailang-core showing ≥22 passed, 0 failed. All other
crates show 0 passed, 0 failed (they have no tests yet). No FAILED lines anywhere.
</verification>

<success_criteria>
- lib.rs declares: node_id, ty, unify, graph (pub), and node_id_tests, ty_tests, unify_tests, graph_tests (#[cfg(test)])
- cargo test --workspace passes with ≥22 tests, 0 failures
- Cargo.toml retains [package] name="ailang-core" version="0.1.0" edition="2021"
- Clean commit pushed to GitHub with message describing the collision fix
- No TypeEnum, Type::option(), or Type::result() references anywhere in the codebase
</success_criteria>

<output>
After completion, create `.planning/quick/260608-f5y-fix-orchestrator-file-corruption-and-ens/260608-f5y-SUMMARY.md`
with: what was fixed, test count before/after, commit hash, and a note about the recurring
lib.rs-collision pattern for future reference.
</output>
